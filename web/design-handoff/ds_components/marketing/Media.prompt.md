Photography placeholder — **the brief shipped no image library**, so every card and hero uses this plate. Replace with real `<img>` when assets land.

```jsx
<Media ratio="16 / 9" label="Coverage dashboard" icon="monitor" />
```
