Closing conversion band. One per page, directly above the footer. `inverse` is the default; `brand` (full coral) is for campaign pages only.

```jsx
<CtaBanner title="See what your test suite is missing." primaryCta="Book a demo" secondaryCta="Talk to an engineer" note="No sales sequence." />
```
