Eyebrow + h2 + deck for every band. Left-aligned by default; centre only above symmetrical grids.

```jsx
<SectionHeader eyebrow="The platform" title="Two testing engines, one queue" description="Agents run the volume. People run the judgment calls." />
```
