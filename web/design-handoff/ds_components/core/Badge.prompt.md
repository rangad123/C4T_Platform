Small mono status pill — release tags, section markers, plan labels, run states.

```jsx
<Badge tone="brand">New</Badge>
<Badge tone="success" dot>All checks passed</Badge>
<Badge tone="inverse" icon="bot">AI agent</Badge>
```

Uppercase mono by default. Use `Tag` instead for content taxonomy (blog categories, resource types).
