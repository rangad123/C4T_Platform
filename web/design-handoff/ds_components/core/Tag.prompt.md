Content-taxonomy pill — blog categories, resource filters, industry chips.

```jsx
<Tag active>All</Tag>
<Tag href="/blog/ai-testing">AI testing</Tag>
```

Interactive filter rows: one `active` tag at a time, ink fill. Non-interactive metadata tags render as a `<span>`.
