Square icon-only control for toolbars, carousels, menu toggles and social rows.

```jsx
<IconButton icon="menu" label="Open menu" />
<IconButton icon="arrow-right" label="Next" variant="outline" size="lg" />
```

`label` is required. Sizes are 32/40/48px — never below 40px for a primary touch target on mobile.
