The Crowd4Test wordmark. **Placeholder** — no logo artwork was supplied, so this is the name set in Instrument Sans with a coral "4".

```jsx
<Logo size={22} />
<Logo size={20} tone="inverse" />
```

Never redraw or restyle it inline; swap this component's internals when real artwork arrives.
