Dark global footer — the only always-inverse surface in the system. Brand column with newsletter on the left, five link columns right, legal + compliance strip beneath.

```jsx
<Footer onNavigate={setPage} />
```
