Numbered pagination for blog and resource indexes. Truncates with an ellipsis beyond one page either side of the current page.

```jsx
<Pagination page={page} pageCount={12} onChange={setPage} />
```
