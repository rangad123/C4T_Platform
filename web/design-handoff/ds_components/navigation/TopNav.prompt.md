The global header: wordmark left, seven primary items, sign-in + coral demo CTA right. Mega menus open on hover; below 1100px everything collapses to a hamburger sheet.

```jsx
<TopNav active="Platform" onNavigate={setPage} announcement="State of AI Quality 2026 — read the report" />
```

Ships with the real Crowd4Test IA baked in via `DEFAULT_NAV`; pass `items` only to change it.
