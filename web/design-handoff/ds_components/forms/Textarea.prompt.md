Multi-line input, vertical resize only. Default 5 rows — enough for a "tell us about your project" field.

```jsx
<Field label="What are you testing?" htmlFor="brief"><Textarea id="brief" rows={5} /></Field>
```
