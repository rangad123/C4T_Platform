Single-select option. Coral ring when selected — the one form control that uses the accent.

```jsx
<Radio name="plan" label="Scale" description="For teams shipping weekly" checked={p==='scale'} onChange={...} />
```
