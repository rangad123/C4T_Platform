48px text input with a hairline border; focus flips the border to ink with a soft ring (no coral glow).

```jsx
<Field label="Work email" htmlFor="email"><Input id="email" type="email" placeholder="you@company.com" /></Field>
<Input iconLeft="search" placeholder="Search resources" />
```
