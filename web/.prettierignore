.next/
node_modules/
out/
next-env.d.ts
package-lock.json

# Read-only design handoff — the browser prototype and the design-system
# sources we port FROM. Kept byte-identical so they stay diffable against a
# future handoff revision.
design-handoff/

# Design tokens are COPIED VERBATIM from the handoff (see CLAUDE.md rule 1 and
# the PORT NOTE headers in colors.css / fonts.css / base.css). Reformatting them
# would break that guarantee and turn every future handoff diff into noise.
# The three files carrying deliberate edits are excluded for the same reason —
# their diff against the original must stay minimal and readable.
src/styles/tokens/
