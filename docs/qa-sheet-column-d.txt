
FIXED — the filter is correct; the table was not. Verified with the tester account you sent: no filter and "Active" both return their two projects, "Completed" and "Removed" correctly return none, because all three of their assignments are ACTIVE. The trap was that two columns were both headed "Project" — the title, and a status badge. Every migrated project reads COMPLETED (legacy has no project-status column, so they all came across finished), so you saw a column headed "Project" saying "Completed", filtered "Your status" for Completed, and got nothing. The status column is now headed "Project status". Your own standing on the work is a different thing, and for 2,455 assignments it is ACTIVE — legacy test_status 1 is "Assigned by Admin" and 2 is "Submitted by tester", so a tester assigned who never submitted is genuinely not finished.
FIXED — but the cause was not migration. The 5,469 TesterBrowser rows were always there (legacy user_browsers holds 6,096, of which 228 are soft-deleted); the Browsers page was calling the DEVICES endpoint and filtering on TesterDevice.browser, a column nothing has ever written, so it returned nothing. There is now a real GET /v1/testers/browsers over the browser records, returning browser, version and OS separately. Verified live: 5,469 rows.
FIXED — projectsCompletedCount counts distinct projects with a COMPLETED assignment, but the recompute only ran after bug and rating writes, never when an assignment changed. Completing a tester's work did not move the figure and un-completing it did not either, so the dashboard kept whatever number the last bug they filed left behind. It now refreshes whenever an assignment status changes, and when a revived roster row loses its COMPLETED status.



FIXED — the API always refused an end date before the start, but as the last thing to see the form: you filled in scope and tester requirements first, then got a generic "some values were not accepted" that did not name the dates. The wizard now holds you on the step that owns those fields and says "The test cannot end before it starts", and the end-date picker takes a minimum once a start date is set. Verified on production.
FIXED — and it was far bigger than Argentina. The legacy users table stores country TWICE and the migration read the empty one: usr_country is set on 303 of 6,665 rows, country_flag on 5,863. The old sign-up form used intl-tel-input, which records its flag as a CSS class ("iti-flag ar"). So 5,736 testers arrived with no country at all. Backfilled on production: testers with a country went from ~300 to 5,811 of 6,369, and Argentina now has exactly 20 — matching the legacy data one for one. Top countries now read IN 3,445, EG 203, US 166, GB 105, ID 104, BR 94.
NEEDS SPECIFICS — 6,369 tester profiles migrated from 6,665 users. If particular testers are missing, send a few names or emails and they can be traced individually. Note the old site is still live and taking signups (legacy users went 6,665 to 6,710 during this work), so a freeze before the final cutover run is needed or the gap will keep growing.
CORRECTED, AND NOW LARGELY FIXED — my earlier answer said these were never collected, on the grounds that the legacy organisation table has no column for them. That was wrong and you were right to push back. In the old model a "Company" was a USER (role 2) and the organisation row was little more than a grouping, so the company's email, phone and city live on the users table, on the account that created the organisation. Backfilled on production: contact email now on 265 of 276 organisations, phone on 251, city on 70, country on 205. Your levelply 2 example now reads madduridevi2@gmail.com / 9440697154 / Hyderabad. Website, state and postal code do genuinely have no column anywhere in the legacy schema and cannot be recovered — those have to be collected going forward.
NOT RECOVERABLE — re-checked after the correction above. Unlike email and phone, Tax ID has no column on the legacy organisation table OR on the users table; the old portal never captured it anywhere. It has to be collected going forward. The field exists on the new organisation profile and is editable, so customers can fill it in themselves.
PARTLY FIXED — City is now populated on 70 organisations from the owner's record (see the first organisation row above). "Hyderabad" in Address Line 1 is correct: legacy holds ONE free-text address field, org_address, populated on 76 of 276, and that is what landed there. Address Line 2, State and Postal Code never existed as separate legacy fields, so there is nothing to migrate into them.
CORRECTED — this was right and my first answer was not. Organisation information WAS available in the previous portal, on the owning customer's user record rather than the organisation record, and the migration was only reading the latter. Now backfilled: 265 organisations gained a contact email, 251 a phone, 205 a country, 70 a city. What remains missing (website, state, postal code, Tax ID) is genuinely absent from the legacy database, verified column by column.


FIXED — progress_percent is Int default 0 and the loader never set it, so all 257 projects read "Completed" beside a bar at 0%. The legacy projects table has no status column at all, which is why every migrated project lands COMPLETED; 100% is then the consistent value and is exactly what the platform writes when it completes a project itself. Backfilled: 257 projects to 100%. Separately, this uncovered a crash sitting next to it — reopening a completed project wrote null to that NOT NULL column, and since COMPLETED to IN_PROGRESS is the only move that status has, it was on the path of the only status change available on the platform. Also fixed.
FIXED — a legacy build names its test document through builds.doc_id, with the filename on the documents row it points at. Nothing read the column, so testDocumentFileId was null on all 789 builds. 108 documents are now attached and 44 of them have their bytes, recovered from the old host and uploaded to S3. Verified live: a build with bytes serves a real 264,778-byte download; the other 64 read "no longer available" rather than offering a link that fails.
THE NEW PORTAL IS CORRECT — please re-check the old one. "Managers" here is the users list filtered to role ADMIN or SUB_ADMIN, and the legacy data has 33 users with role 3 (Manager) plus 11 with role 8 (Sub Admin), all flagged active. That is 44, of which 41 migrated as SUB_ADMIN, plus 2 ADMIN, giving the 43 you see. The old portal's screen showing 4 was displaying a narrower slice — it was not filtering on the active flag, since all 45 are active. No data was invented; if you want the list narrowed, tell us which legacy role should count as a Manager.
FIXED — the tester's name on Admin to Devices, and on Admin to Browsers, now links to that tester's profile.
FIXED — and the old values were worse than a mismatch. The loader read devices.dvc_os_details straight into the OS field, and much of that free-text column holds the day the device was added, so a Dell Inspiron read "11-03-2015" and an iPhone 5s "10-05-2015". The OS version was never written at all, null on all 5,917 devices. The real value was in dvc_mob_os_ver_id, which resolves for 5,888 of them. Both now come from that reference: OS name went from 2,196 to 5,917 populated and OS version from 0 to 5,888, reading Android 3,751, iOS 1,694, Windows 430. Samples now say "LG G Pro Lite - Android 4.1 (Jelly Bean)" and "Apple iPhone 5s - iOS 10.3".
FIXED — bug_device_used and bug_browsers_used look like free text and are not: they are ids into devices and user_browsers. The loader read them across as-is, so bugs displayed "588" where the device belongs and "512" where the browser belongs, on 11,729 and 9,382 rows. Both are now resolved to names, including the 229 bugs that hold a LIST of ids ("13,14,512") because they were filed against several devices or browsers. Verified: zero numeric values remain. Devices now read "Samsung Galaxy S8", "Xiaomi Redmi Note 4"; browsers read "IE 11", "Chrome 53", "Firefox 57.0", and multi-browser reports read "Chrome 85, IE 11, Safari 12". As a bonus the bug's OS is now filled on 4,142 rows (it was null on all 21,554) — bugs_report has no OS column, but user_browsers.os_id records the OS the tester registered that browser on, which is the only place it survives.
FIXED — see the Tester Login to Browser row above; this was the same root cause. The Browsers page called the devices endpoint and filtered on a column nothing writes, so it showed nothing while 5,469 browser records sat in the database. It now has its own endpoint and lists all 5,469, with the tester's name linking to their profile.
