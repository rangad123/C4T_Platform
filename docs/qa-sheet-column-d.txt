Dev Review
FIXED — the filter is correct; the table was not. Verified with the tester account you sent: unfiltered and ACTIVE both return their two projects, while COMPLETED and REMOVED correctly return none, because all three of their assignments are ACTIVE. The table was the problem: TWO columns were headed "Project" — the title, and a status badge. Every migrated project reads COMPLETED (the legacy schema has no project status column, so they all came across finished), so you saw a column headed "Project" reading "Completed", filtered "Your status" for Completed, and were told nothing matched. Your own standing on the work is a different thing, and for 2,455 assignments it is ACTIVE. The status column is now headed "Project status".
FIXED — but the cause was not migration. The 5,469 TesterBrowser rows were there all along (legacy user_browsers holds 6,096, of which 228 are soft-deleted). Two real bugs hid them: a browser's operating system was never written despite the reference resolving, and the Browsers page queried the devices endpoint on a column nothing populates. Both fixed.
FIXED — projectsCompletedCount counts distinct projects with a COMPLETED assignment, but the recompute ran only after bug and rating writes, never when an assignment changed. Completing a tester's work did not move the figure and un-completing it did not either. It is now refreshed whenever an assignment status changes, and when a revived roster row loses its COMPLETED status.



FIXED — the API always refused an end date before the start, but as the last thing to see the form: you filled in scope and tester requirements first, then got a generic "some values were not accepted" that never named the dates. The message "The test cannot end before it starts" had been sitting unused in the code with nothing to raise it. The step that owns those fields now holds you there, and the end-date picker takes a minimum once a start date is known. Verified on both portals; equal dates are still allowed, as a one-day test should be.
FIXED — and it was far bigger than Argentina. The legacy users table stores country TWICE and the migration read the empty one: usr_country is set on 348 of 6,710 rows, country_flag on 5,863. So 5,736 testers arrived with no country and no country filter matched anything. Backfilled: testers with a country went from about 300 to 5,811, and Argentina now returns 19 on the live customer portal. All three legacy sources agree the true figure is 20 — the twentieth is still APPLIED, so customers do not see them.
NEEDS SPECIFICS — 6,369 tester profiles migrated, which matches the legacy count of users in tester roles exactly; your old portal's 6,193 is its own filtered view. Nothing is missing at the table level. What reduces the customer-facing list is the verification gate: 6,000 of 6,369 are VERIFIED and only those are shown. If a particular tester you expect is absent, send a name or email and I will trace that one record end to end.
CORRECTED, AND NOW LARGELY FIXED — my earlier answer said these were never collected, because the legacy organisation table has no column for them. That was wrong and you were right to push back. In the old model a "Company" was a USER (role 2) and the organisation row was little more than a grouping, so the company's email, phone and city live on the users table, on the account that created the organisation. Backfilled: contact email now on 265 of 276 organisations, phone on 251, city on 70, country on 205. Your levelply 2 example now reads madduridevi2@gmail.com / 9440697154 / Hyderabad. Website and state genuinely have no column anywhere in legacy and must be collected going forward.
NOT RECOVERABLE — re-checked after the correction above. Unlike email and phone, Tax ID has no column on the legacy organisation table OR on the users table; the old portal never captured it anywhere. It has to be collected going forward. The field exists on the new organisation profile and is editable, so customers can fill it in themselves.
PARTLY FIXED — City is now populated on 70 organisations from the owner's record. "Hyderabad" alone in Address Line 1 is correct: legacy holds ONE free-text address field, org_address, populated on 76 of 276, and that is what landed there. Address Line 2, State and Postal Code never existed as separate legacy fields, so there is nothing to migrate into them.
CORRECTED — this was right and my first answer was not. Organisation information WAS available in the previous portal, on the owning customer's user record rather than the organisation record, and the migration read only the latter. Now backfilled: 265 organisations gained a contact email, 251 a phone, 205 a country, 70 a city. What remains missing (website, state, postal code, Tax ID) is genuinely absent from the legacy database, verified column by column.
NOT REPRODUCIBLE — the work history renders. Verified live on your own customer account: opening a tester who has worked for your organisation shows "What this tester has done" with the project linked. Your organisation (eglownetworks) has 415 assignments across 218 distinct testers, so the data is there. The section is deliberately scoped to YOUR projects — a customer should not see a tester's work for other customers — so a tester who has never worked for you correctly reads "This tester has not worked on any of your projects yet". If you opened one of those, that is the message you saw. Send a tester name if you believe otherwise.
FIXED — the customer tester page now uses the same four tabs as the admin one, with the same names: Profile, Assets, Skills, Work history. It was one long column of six panels, which reads fine for a tester with two devices and one engagement and stops reading at all for a busy one — the work history grows without bound, so devices and browsers ended up far below the fold of a page that gave no hint they were there. It reuses the same SectionTabs component the admin page uses, so the two portals now describe the same record the same way. One detail worth noting: the rating dialog is raised from a Work history row and is driven by a query param, so both the Rate link and its close action carry the tab with them — otherwise rating a tester would silently drop you back to Profile. Verified live on your own customer account: each tab renders only its own panels.


FIXED — a legacy build names its test document through builds.doc_id, with the filename on the documents row it points at. Nothing read the column, so testDocumentFileId was null on all 789 builds and every portal showed a blank. 108 documents are now attached and 44 of them have their bytes, recovered from the old host and uploaded to S3. Verified live: a build with bytes serves a real 264,778-byte download; the other 64 read "no longer available" rather than offering a link that fails.
THE NEW PORTAL IS CORRECT — please re-check the old one. "Managers" here is the SUB_ADMIN role, and the legacy database holds 33 users in the Manager role plus 11 Sub Admins, all active. 43 is the honest count; 4 is what the old portal's own filtered view showed. Nothing was invented in migration. Separately, see the deactivation row below — the reason they all read Active is that the old platform recorded no deactivation for any of them.
FIXED — the tester's name on Admin to Devices, and on Admin to Browsers, now links through to that tester's record. They were plain text before.
FIXED — and the old values were worse than a mismatch. The loader read devices.dvc_os_details straight into osName, and much of that free-text column holds the day the device was added, so a Dell Inspiron arrived running "11-03-2015" and an iPhone 5s "10-05-2015"; osVersion was never written at all. The real value was in dvc_mob_os_ver_id, which resolves for 5,888 of 5,917 devices. Both columns now come from that reference: os_name went from 2,196 to 5,917 and os_version from 0 to 5,888, reading Android 3,751, iOS 1,694, Windows 430.
FIXED — bug_device_used and bug_browsers_used are legacy IDs, and the loader stored them as text, so every bug displayed a number: device "588", browser "512". They are now resolved to real names through the devices and browser tables.
FIXED — the Browsers page was querying the devices endpoint filtered on a column nothing populates, so it always returned empty. It now reads the browser catalogue. Same root cause as the Tester Login to Browser row above.
FIXED — see the Browsers row above; this was the same root cause. The browser version list is populated: 6 browsers and 635 versions in the catalogue.
FIXED — the capability existed but nothing led to it. A manager IS a user with a SUB_ADMIN role, so the status control, the reason field, the archive action and the last-admin guard all live on their user record; the Managers section simply never linked there. A "Manage account" action on the manager page now does. On why they all read Active: the legacy platform recorded no deactivation for any of them, so that is the migrated truth rather than a default.
FIXED — the tab is renamed "Assets" and now shows Browsers beside Devices, exactly as asked. The cause was in the API: the admin tester read returned devices and not browsers, though the discoverable-tester read has carried them all along. Verified the endpoint now returns both.
FIXED — same change as the row above. Clicking a tester's name on Admin to Browsers landed on a page with no browser section on it; that page now has one, under Assets.
NOT A DEFECT — a design preference, and a reasonable one. Worth noting it is a larger change than it looks: every list on the platform shares one Table component and one filter bar, so a card view means a second presentation for all of them plus a toggle whose state has to live in the URL to survive paging and filtering. Happy to build it — tell me whether you want it everywhere or only on Testers, Managers and Projects, and whether list or cards should be the default.
